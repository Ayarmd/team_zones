# =============================================================================
# Vercel — React dashboard (zones_react) — FREE, no credit card
# =============================================================================

# Render API:
VITE_API_BASE_URL=https://zones-api.onrender.com/api

# OR Railway API:
# VITE_API_BASE_URL=https://YOUR-SERVICE.up.railway.app/api

# Vercel project settings:
#   Root Directory: zones_react
#   Framework: Vite (vercel.json)
#   Build: npm run build | Output: dist
