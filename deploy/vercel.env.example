# =============================================================================
# Vercel — React dashboard (zones_react) — jeje430/myzones
# =============================================================================
# Import: https://github.com/jeje430/myzones
# Guide:  vercel/README.md
# Root:   empty (uses /vercel.json) OR zones_react (uses zones_react/vercel.json)
# =============================================================================

# Laravel API (Render / CyberPanel / Railway):
VITE_API_BASE_URL=https://api.yourdomain.com/api

# Render example:
# VITE_API_BASE_URL=https://zones-api.onrender.com/api

# Vercel project settings:
#   Root Directory: (empty) or zones_react
#   Framework: Vite
#   Build: npm run build | Output: zones_react/dist or dist
